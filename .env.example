# Copiar a .env (dev). Ver "VITE_API_URL por entorno" en src/GUIA_FRONT.md.
# Dev / prod con Nitro: el front llama al proxy server-side (sin CORS).
VITE_API_URL=/api/ords/
# Destino del proxy (server-side).
ORDS_TARGET=https://oracleapex.com
# App id por defecto para menu/paginas
VITE_APP_ID=86972
